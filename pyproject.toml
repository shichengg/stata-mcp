[project]
name = "stata-mcp"
version = "0.1.0"
description = "Stata Automation COM MCP server for Claude Code"
requires-python = ">=3.10"
dependencies = [
  "mcp",
  "pywin32; platform_system == 'Windows'",
]

[project.scripts]
stata-mcp = "stata_mcp.server:main"
